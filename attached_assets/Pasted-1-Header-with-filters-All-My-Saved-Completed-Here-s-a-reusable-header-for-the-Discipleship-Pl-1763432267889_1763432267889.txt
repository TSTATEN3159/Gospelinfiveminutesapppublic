1️⃣ Header with filters: All / My Saved / Completed
Here’s a reusable header for the Discipleship Plans screen.
A. Type + header component
// client/src/features/discipleship/DiscipleshipPlansHeader.tsx

export type PlansFilter = "all" | "saved" | "completed";

interface DiscipleshipPlansHeaderProps {
  activeFilter: PlansFilter;
  onChangeFilter: (filter: PlansFilter) => void;
  counts?: {
    all?: number;
    saved?: number;
    completed?: number;
  };
}

export function DiscipleshipPlansHeader({
  activeFilter,
  onChangeFilter,
  counts,
}: DiscipleshipPlansHeaderProps) {
  const baseButton =
    "px-3 py-1.5 rounded-full text-xs font-medium border transition-colors";
  const activeClasses =
    "bg-slate-900 text-white border-slate-900 shadow-sm";
  const inactiveClasses =
    "bg-white text-slate-700 border-slate-300 hover:bg-slate-50";

  const pill = (filter: PlansFilter, label: string, count?: number) => {
    const isActive = activeFilter === filter;
    return (
      <button
        key={filter}
        type="button"
        onClick={() => onChangeFilter(filter)}
        className={`${baseButton} ${isActive ? activeClasses : inactiveClasses}`}
      >
        <span>{label}</span>
        {typeof count === "number" && (
          <span className="ml-1 text-[10px] opacity-75">
            ({count})
          </span>
        )}
      </button>
    );
  };

  return (
    <div className="w-full mb-4">
      <div className="flex items-center justify-between mb-2">
        <h1 className="text-lg font-semibold text-slate-900">
          Discipleship Plans
        </h1>
      </div>

      <div className="flex items-center gap-2 overflow-x-auto no-scrollbar">
        {pill("all", "All Plans", counts?.all)}
        {pill("saved", "My Saved Plans", counts?.saved)}
        {pill("completed", "Completed Plans", counts?.completed)}
      </div>
    </div>
  );
}
All Plans → shows everything in DISCIPLESHIP_PLANS
My Saved Plans → plans the user has started or manually saved
Completed Plans → plans where all days are finished
B. Example: wiring it up on the main Discipleship screen
This shows how to make it actually do something:
// client/src/features/discipleship/DiscipleshipPlansScreen.tsx

import { useMemo, useState } from "react";
import {
  DISCIPLESHIP_PLANS,
  DiscipleshipPlan,
} from "@/features/discipleship/discipleshipPlans";
import {
  DiscipleshipPlansHeader,
  PlansFilter,
} from "@/features/discipleship/DiscipleshipPlansHeader";

// EXAMPLE progress shape – Replit can replace this with real state/store
interface PlanProgress {
  planId: string;
  daysCompleted: number;
  totalDays: number;
  isSaved: boolean; // user tapped “save / bookmark” or has started it
}

function useExamplePlanProgress(): PlanProgress[] {
  // Replit should replace this with real data (localStorage / backend)
  return [
    // sample shape:
    // { planId: "heaven-or-hell", daysCompleted: 2, totalDays: 5, isSaved: true },
  ];
}

function filterPlansByProgress(
  plans: DiscipleshipPlan[],
  progress: PlanProgress[],
  filter: PlansFilter
): DiscipleshipPlan[] {
  if (filter === "all") return plans;

  const progressMap = new Map<string, PlanProgress>();
  for (const p of progress) {
    progressMap.set(p.planId, p);
  }

  if (filter === "saved") {
    return plans.filter((plan) => {
      const p = progressMap.get(plan.id);
      // “saved” means: user has explicitly saved it OR started it (daysCompleted > 0)
      return p && (p.isSaved || p.daysCompleted > 0) && p.daysCompleted < (p.totalDays || plan.days.length);
    });
  }

  if (filter === "completed") {
    return plans.filter((plan) => {
      const p = progressMap.get(plan.id);
      const total = p?.totalDays ?? plan.days.length;
      return p && p.daysCompleted >= total && total > 0;
    });
  }

  return plans;
}

export default function DiscipleshipPlansScreen() {
  const [filter, setFilter] = useState<PlansFilter>("all");
  const progress = useExamplePlanProgress(); // replace with real hook

  const counts = useMemo(() => {
    const all = DISCIPLESHIP_PLANS.length;
    const saved = filterPlansByProgress(DISCIPLESHIP_PLANS, progress, "saved")
      .length;
    const completed = filterPlansByProgress(
      DISCIPLESHIP_PLANS,
      progress,
      "completed"
    ).length;
    return { all, saved, completed };
  }, [progress]);

  const visiblePlans = useMemo(
    () => filterPlansByProgress(DISCIPLESHIP_PLANS, progress, filter),
    [progress, filter]
  );

  return (
    <div className="px-4 pb-6">
      <DiscipleshipPlansHeader
        activeFilter={filter}
        onChangeFilter={setFilter}
        counts={counts}
      />

      <div className="mt-4 space-y-3">
        {visiblePlans.map((plan) => (
          <button
            key={plan.id}
            type="button"
            // your navigation handler here
            // onClick={() => navigateToPlan(plan.id)}
            className="w-full text-left rounded-2xl border border-slate-200 bg-white p-4 shadow-sm active:scale-[0.99] transition-transform"
          >
            <div className="flex gap-3">
              <div className="h-14 w-14 rounded-xl overflow-hidden bg-slate-100 flex-shrink-0">
                {/* Replit can wire real <img /> / <Image /> */}
              </div>
              <div className="flex flex-col justify-center">
                <div className="text-sm font-semibold text-slate-900">
                  {plan.title}
                </div>
                {plan.subtitle && (
                  <div className="text-xs text-slate-500">
                    {plan.subtitle}
                  </div>
                )}
              </div>
            </div>
          </button>
        ))}
      </div>
    </div>
  );
}
🔑 Replit just needs to swap useExamplePlanProgress for your real progress storage, and wire up the navigation on click. The header and filters will then be fully functional.