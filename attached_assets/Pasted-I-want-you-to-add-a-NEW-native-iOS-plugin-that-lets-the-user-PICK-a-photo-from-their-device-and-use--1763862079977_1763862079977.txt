I want you to add a NEW native iOS plugin that lets the user PICK a photo from their device and use it as a CUSTOM BACKGROUND IMAGE for Scripture cards.

Goal:
- On iOS: user taps “Use my photo” (or similar)
- Native photo picker opens
- User selects a photo
- Plugin saves it to app storage and returns a file URL to JavaScript
- React/TS uses that URL as a background for Scripture images (and/or stores it in the user’s custom backgrounds list)

Please follow these steps EXACTLY:

━━━━━━━━━━━━━━━━━━
📌 1️⃣ Create BackgroundImagePickerPlugin.swift
━━━━━━━━━━━━━━━━━━
In the iOS Xcode project (App target), create a new Swift file:

BackgroundImagePickerPlugin.swift

Paste THIS code into it:

import Capacitor
import UIKit

@objc(BackgroundImagePickerPlugin)
public class BackgroundImagePickerPlugin: CAPPlugin, CAPBridgedPlugin, UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    public let identifier = "BackgroundImagePickerPlugin"
    public let jsName = "BackgroundImagePicker"
    public let pluginMethods: [CAPPluginMethod] = [
        CAPPluginMethod(name: "pickImage", returnType: CAPPluginReturnPromise)
    ]

    private var currentCall: CAPPluginCall?

    @objc func pickImage(_ call: CAPPluginCall) {
        DispatchQueue.main.async {
            guard let viewController = self.bridge?.viewController else {
                call.reject("No root view controller available")
                return
            }

            self.currentCall = call

            let picker = UIImagePickerController()
            picker.sourceType = .photoLibrary
            picker.delegate = self
            picker.allowsEditing = false

            viewController.present(picker, animated: true, completion: nil)
        }
    }

    public func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
        picker.dismiss(animated: true) {
            self.currentCall?.reject("User cancelled image picking")
            self.currentCall = nil
        }
    }

    public func imagePickerController(_ picker: UIImagePickerController,
                                      didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey : Any]) {
        picker.dismiss(animated: true) {
            guard let call = self.currentCall else { return }

            guard let image = info[.originalImage] as? UIImage else {
                call.reject("Failed to get image")
                self.currentCall = nil
                return
            }

            // Save the picked image into app’s temporary directory
            guard let data = image.jpegData(compressionQuality: 0.9) else {
                call.reject("Failed to convert image to JPEG")
                self.currentCall = nil
                return
            }

            let filename = "user_background_\(UUID().uuidString).jpg"
            let tempDir = FileManager.default.temporaryDirectory
            let fileURL = tempDir.appendingPathComponent(filename)

            do {
                try data.write(to: fileURL)
                call.resolve([
                    "fileUrl": fileURL.absoluteString
                ])
            } catch {
                call.reject("Failed to save image: \(error.localizedDescription)")
            }

            self.currentCall = nil
        }
    }
}

━━━━━━━━━━━━━━━━━━
📌 2️⃣ Register Plugin In AppDelegate.swift
━━━━━━━━━━━━━━━━━━
In App/App/AppDelegate.swift (or the file where capacitorDidLoad() is located), REGISTER this plugin as well.

Inside capacitorDidLoad(), add:

bridge?.registerPluginInstance(BackgroundImagePickerPlugin())

The method should now look roughly like:

override func capacitorDidLoad() {
    super.capacitorDidLoad()
    bridge?.registerPluginInstance(ShareCardPlugin())
    bridge?.registerPluginInstance(VerseNotificationsPlugin())
    bridge?.registerPluginInstance(ScriptureImagePlugin())
    bridge?.registerPluginInstance(BackgroundImagePickerPlugin())
}

━━━━━━━━━━━━━━━━━━
📌 3️⃣ Add TypeScript Wrapper
━━━━━━━━━━━━━━━━━━
Create a new file:

client/src/plugins/background-image-picker.ts

Paste THIS:

import { registerPlugin } from '@capacitor/core';

export interface BackgroundImagePickerPlugin {
  pickImage(): Promise<{ fileUrl: string }>;
}

const BackgroundImagePicker = registerPlugin<BackgroundImagePickerPlugin>(
  'BackgroundImagePicker'
);

export default BackgroundImagePicker;

━━━━━━━━━━━━━━━━━━
📌 4️⃣ Integrate With Scripture Image / Background UI
━━━━━━━━━━━━━━━━━━
Find the React/TS component that handles SCRIPTURE IMAGE BACKGROUNDS (where the user currently selects from built-in background images or uploads their own image in the web implementation).

In that file:

1. Import the plugin:

import BackgroundImagePicker from '@/plugins/background-image-picker';
import { Capacitor } from '@capacitor/core';

2. Add a handler function that will be used on native iOS:

const handlePickNativeBackground = async () => {
  try {
    // Only do this on native iOS; keep existing web upload flow intact
    if (!Capacitor.isNativePlatform() || Capacitor.getPlatform() !== 'ios') {
      console.warn('Native background picker is only available on iOS.');
      return;
    }

    const { fileUrl } = await BackgroundImagePicker.pickImage();

    // Use the same state / structure you currently use to handle custom backgrounds.
    // For example, if you track backgrounds as an array, push this one in.
    // Or set it as the current background directly.

    // Example (adjust to match your actual state):
    // setCustomBackgrounds((prev) => [...prev, { id: fileUrl, url: fileUrl, isUser: true }]);
    // setSelectedBackgroundId(fileUrl);

    console.log('Picked background image:', fileUrl);
  } catch (e) {
    console.error('Failed to pick native background image', e);
  }
};

3. Add a button in the Scripture Image / background selector UI for iOS that calls this handler.

Place it near your existing background image controls. Example:

<Button
  variant="outline"
  size="sm"
  onClick={handlePickNativeBackground}
>
  Use my photo (iOS)
</Button>

4. IMPORTANT:
Do NOT remove your existing web-based “upload your own image” logic.
Instead:
- Use the native plugin when running on iOS native (Capacitor.isNativePlatform() && platform === 'ios')
- Keep the HTML file input / existing upload mechanism for web

If you already have a function for handling custom user images, plug the native `fileUrl` into that same flow so that native and web share the same logic from that point onward.

━━━━━━━━━━━━━━━━━━
📌 5️⃣ Sync & Test
━━━━━━━━━━━━━━━━━━
Run:

npx cap sync ios

Then open the iOS project in Xcode, run on a simulator or device:

1. Go to the Scripture Image Generator / background picker screen.
2. Tap “Use my photo (iOS)” button.
3. Confirm:
   - The native photo picker opens.
   - You can choose an image from Photos.
   - After picking, the chosen image appears as a background option.
   - Generating a Scripture image uses this background like your other built-in backgrounds.

━━━━━━━━━━━━━━━━━━
That’s all. Please implement this now and tell me when it’s complete.
