1. What’s going wrong right now?

A few subtle problems in your current TextScaleContext:

Touch handlers re-bind every time scale changes

useEffect(() => {
  // ... define lastDistance, handlers ...
  window.addEventListener("touchstart", onTouchStart, { passive: true });
  window.addEventListener("touchmove", onTouchMove, { passive: true });
  window.addEventListener("touchend", onTouchEnd);
  return () => {
    window.removeEventListener("touchstart", onTouchStart, { passive: true });
    window.removeEventListener("touchmove", onTouchMove, { passive: true });
    window.removeEventListener("touchend", onTouchEnd);
  };
}, [scale]); // 👈 this is the problem


Every time you pinch, setScale fires → React re-renders → the effect tears down and re-attaches all listeners, resetting lastDistance mid-gesture. On desktop you might still see some change; on a real device with more jank, it can feel like “nothing works.”

The handlers close over a snapshot of scale

Inside onTouchMove you do:

const nextScale = scale + (delta * STEP) / 100;
setScale(nextScale);


Because scale comes from the closure created when the effect ran, it can lag behind the actual current value during continuous pinch.

Using passive: true is fine if you don’t want to block native zoom
But if you ever decide to prevent the page itself from zooming and only scale text, you must use passive: false and call preventDefault() on the two-finger touches. iOS & MDN explicitly call this out. 
MDN Web Docs
+1

Right now you’re not preventing default, so that’s not the direct bug—but it does mean that in a WKWebView the system pinch-zoom can “win” visually even if your CSS variable is changing.

2. What Apple actually says about text enlargement

Apple’s official guidance for text size is Dynamic Type:

“The Dynamic Type feature allows users to choose the size of textual content displayed on the screen…” 
Apple Developer
+1

HIG typography: layouts should adapt gracefully to different font sizes and remain legible. 
Apple Developer

You’re essentially building an in-app Dynamic Type slider driven by pinch:

All text scales from a base size (you’re using html { font-size: calc(16px * var(--text-scale)); }), which is aligned with Apple’s ~17pt body-text guideline. 
Glance

Users can adjust it on demand (pinch + buttons), which is in the spirit of Dynamic Type.

There is no special “pinch text plugin” from Apple for web content. In a Capacitor/WKWebView app, Apple’s “directives” boil down to:

Respect accessibility by allowing text to get larger and remain readable. 
Apple Developer
+2
Apple Developer
+2

Don’t trap the user in unreadable text sizes or unexpected zoom behavior.

For web content, if you handle gestures yourself, use the standard touch/pointer patterns (e.g., touch-action, preventDefault when necessary). 
MDN Web Docs
+1

So we’ll keep your architecture (CSS variable + rem units) but implement the pinch in a way iOS likes.

3. Fixed & iOS-friendly TextScaleContext.tsx

Here’s a revised version that:

Attaches touch listeners once.

Uses refs so handlers always see the current scale.

Optionally prevents the system from also zooming the whole page when two fingers are down.

Still persists to localStorage and updates --text-scale.

// client/src/context/TextScaleContext.tsx
import {
  createContext,
  useContext,
  useEffect,
  useRef,
  useState,
  ReactNode,
} from "react";

type TextScaleContextValue = {
  scale: number;
  setScale: (value: number) => void;
};

const TextScaleContext = createContext<TextScaleContextValue>({
  scale: 1,
  setScale: () => {},
});

const MIN_SCALE = 0.8; // 80%
const MAX_SCALE = 1.6; // 160%
const STEP = 0.02;     // sensitivity

export const TextScaleProvider: React.FC<{ children: ReactNode }> = ({
  children,
}) => {
  // initial from localStorage
  const [scale, setScaleState] = useState<number>(() => {
    if (typeof window === "undefined") return 1;
    const stored = window.localStorage.getItem("text-scale");
    const num = stored ? Number(stored) : 1;
    return Number.isFinite(num) ? num : 1;
  });

  // keep the latest scale in a ref so touch handlers always see it
  const scaleRef = useRef(scale);
  useEffect(() => {
    scaleRef.current = scale;
  }, [scale]);

  const applyScaleToDocument = (value: number) => {
    if (typeof document !== "undefined") {
      document.documentElement.style.setProperty(
        "--text-scale",
        String(value)
      );
    }
  };

  const setScale = (value: number) => {
    const clamped = Math.min(MAX_SCALE, Math.max(MIN_SCALE, value));
    setScaleState(clamped);
    if (typeof window !== "undefined") {
      window.localStorage.setItem("text-scale", String(clamped));
    }
    applyScaleToDocument(clamped);
  };

  // initialize CSS variable on mount
  useEffect(() => {
    applyScaleToDocument(scaleRef.current);
  }, []);

  // Global two-finger pinch handler
  useEffect(() => {
    if (typeof window === "undefined") return;

    let lastDistance: number | null = null;

    const getDistance = (touches: TouchList) => {
      const t1 = touches[0];
      const t2 = touches[1];
      const dx = t2.clientX - t1.clientX;
      const dy = t2.clientY - t1.clientY;
      return Math.hypot(dx, dy);
    };

    const handleTouchStart = (e: TouchEvent) => {
      if (e.touches.length === 2) {
        lastDistance = getDistance(e.touches);
        // Optional: prevent the system from starting its own page zoom
        e.preventDefault();
      }
    };

    const handleTouchMove = (e: TouchEvent) => {
      if (e.touches.length === 2 && lastDistance !== null) {
        const distance = getDistance(e.touches);
        const delta = distance - lastDistance;

        const current = scaleRef.current;
        const next = current + (delta * STEP) / 100;

        setScale(next);
        lastDistance = distance;

        // Optional: keep the system from also zooming the webview
        e.preventDefault();
      }
    };

    const handleTouchEnd = (e: TouchEvent) => {
      if (e.touches.length < 2) {
        lastDistance = null;
      }
    };

    // Important: passive must be false if we call preventDefault()
    const options: AddEventListenerOptions = { passive: false };

    document.addEventListener("touchstart", handleTouchStart, options);
    document.addEventListener("touchmove", handleTouchMove, options);
    document.addEventListener("touchend", handleTouchEnd, options);
    document.addEventListener("touchcancel", handleTouchEnd, options);

    return () => {
      document.removeEventListener("touchstart", handleTouchStart, options as any);
      document.removeEventListener("touchmove", handleTouchMove, options as any);
      document.removeEventListener("touchend", handleTouchEnd, options as any);
      document.removeEventListener("touchcancel", handleTouchEnd, options as any);
    };
  }, []); // 👈 attach once, no dependency on scale

  return (
    <TextScaleContext.Provider value={{ scale, setScale }}>
      {children}
    </TextScaleContext.Provider>
  );
};

export const useTextScale = () => useContext(TextScaleContext);

Why this behaves better in TestFlight / WKWebView

Single registration: iOS isn’t constantly tearing down and re-installing handlers mid-pinch.

Refs instead of stale closures: the math always uses the actual current scale.

Two-finger only + preventDefault(): we only interfere with the system gesture when two touches are present, which matches Apple’s expectation that you don’t break single-finger scrolling and taps. 
MDN Web Docs
+1

If you decide you do want the system’s page zoom to remain available, just remove the two e.preventDefault() calls—your pinch-to-enlarge-text will still work, but the page may also zoom.

4. CSS: play nicely with gestures and keep Apple-style scaling

Your global CSS is already aligned with Apple’s typography guidance (16px base ≈ 17pt). 
Apple Developer
+1

You can keep it, and optionally add a touch-action hint (helps on browsers that use Pointer Events):

/* client/src/index.css */

:root {
  --text-scale: 1;
}

html {
  font-size: calc(16px * var(--text-scale));
}

/* Optional: clarify intent for browsers that support touch-action */
html, body {
  /* allow normal panning; pinch will be handled by JS when two fingers are down */
  touch-action: pan-x pan-y;
}


This is consistent with MDN and browser guidance for custom gesture handling. 
MDN Web Docs

5. Make sure it’s actually mounted in your sandbox shell

You said all features are “wrapped now for sandbox.” In that setup you need to ensure the provider itself is inside whatever sandbox wrapper you’re using in the TestFlight build.

Conceptually, it should look like:

// client/src/main.tsx
import ReactDOM from "react-dom/client";
import App from "./App";
import { TextScaleProvider } from "./context/TextScaleContext";
// whatever your sandbox wrapper is called:
import { SandboxShell } from "./sandbox/SandboxShell";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <SandboxShell>
    <TextScaleProvider>
      <App />
    </TextScaleProvider>
  </SandboxShell>
);


If TextScaleProvider ends up outside or behind a sandbox feature flag that’s disabled in your TestFlight config, the touch handlers simply never mount—so pinch will appear “dead” even though the code is fine.

Double-check that:

The same tree (with TextScaleProvider) is used in your production/TestFlight entry point.

The sandbox doesn’t conditionally skip rendering the provider on real devices.

6. How this lines up with Apple’s directives

Putting it all together, this implementation:

Respects Apple typography & Dynamic Type concepts: your base size and proportional scaling match Apple’s guidance for readable text and adaptable layouts. 
Apple Developer
+3
Apple Developer
+3
Apple Developer
+3

Behaves well with iOS WebKit: we use standard touch patterns (single non-passive listener, optional preventDefault on multi-touch) as recommended for apps that handle gestures themselves. 
MDN Web Docs
+1

Provides an explicit in-app scaling mechanism so users who need larger text aren’t dependent only on system zoom.

If you drop in the revised TextScaleContext.tsx, keep your CSS, and ensure the provider is actually mounted in the sandboxed app tree, your two-finger pinch to enlarge text should start working in TestFlight the way you expect.