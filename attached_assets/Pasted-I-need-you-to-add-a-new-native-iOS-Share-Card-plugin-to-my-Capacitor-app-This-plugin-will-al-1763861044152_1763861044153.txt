I need you to add a **new native iOS Share Card plugin** to my Capacitor app.

This plugin will allow the user to share a scripture as an **image card**, instead of sharing a website link.

Follow all instructions below exactly:

━━━━━━━━━━━━━━━━━━
📌 1️⃣ Create New Swift Plugin
━━━━━━━━━━━━━━━━━━
Create a new Swift file in Xcode inside the App target, named:
ShareCardPlugin.swift

Paste THIS exact code into it:

import Capacitor
import UIKit

@objc(ShareCardPlugin)
public class ShareCardPlugin: CAPPlugin, CAPBridgedPlugin {
    public let identifier = "ShareCardPlugin"
    public let jsName = "ShareCard"
    public let pluginMethods: [CAPPluginMethod] = [
        CAPPluginMethod(name: "share", returnType: CAPPluginReturnPromise)
    ]

    @objc func share(_ call: CAPPluginCall) {
        let verseText = call.getString("verseText") ?? ""
        let reference = call.getString("reference") ?? ""
        
        let text = "\(verseText)\n\n\(reference)"
        
        let activityVC = UIActivityViewController(activityItems: [text], applicationActivities: nil)
        DispatchQueue.main.async {
            UIApplication.shared.windows.first?.rootViewController?.present(activityVC, animated: true, completion: nil)
        }
        
        call.resolve()
    }
}

━━━━━━━━━━━━━━━━━━
📌 2️⃣ Register The Plugin In iOS
━━━━━━━━━━━━━━━━━━
Open the file: App/App/AppDelegate.swift
(or the file containing `capacitorDidLoad()` if different)

Inside `capacitorDidLoad()` add:

bridge?.registerPluginInstance(ShareCardPlugin())

Example:
override func capacitorDidLoad() {
    super.capacitorDidLoad()
    bridge?.registerPluginInstance(ShareCardPlugin())
}

━━━━━━━━━━━━━━━━━━
📌 3️⃣ Add Web/JS Plugin Wrapper
━━━━━━━━━━━━━━━━━━
Create a new file:
client/src/plugins/share-card.ts

Paste THIS:

import { registerPlugin } from '@capacitor/core';

export interface ShareCardPlugin {
  share(options: { verseText: string; reference: string }): Promise<void>;
}

const ShareCard = registerPlugin<ShareCardPlugin>('ShareCard');

export default ShareCard;

━━━━━━━━━━━━━━━━━━
📌 4️⃣ Integrate With Share Button
━━━━━━━━━━━━━━━━━━
Find the Share button logic in the Daily Verse component and replace ONLY the share handler function with:

import ShareCard from '@/plugins/share-card';

const handleShare = async () => {
  try {
     await ShareCard.share({
        verseText: verse.text,
        reference: verse.ref,
     });
  } catch (e) {
     console.error('Share failed', e);
  }
};

Make sure the `Share` button’s onClick calls `handleShare`.

━━━━━━━━━━━━━━━━━━
📌 5️⃣ Build & Test
━━━━━━━━━━━━━━━━━━
- Run npx cap sync ios
- Build/run the app in Xcode on a real iPhone or simulator
- Tap Share → it must open the native iOS share sheet with the scripture text

━━━━━━━━━━━━━━━━━━
End of instructions.
Please update the code for me now.
